const SectionStarter = (props) => {
  return (
    <a className="bg-red-400 rounded-[100px] px-[10px] py-[101px] text-[11px] mb-[100px]">
      {props.message}
    </a>
  );
};

export default SectionStarter;
